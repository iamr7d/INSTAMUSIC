{
  "name": "instamatch",
  "version": "1.0.0",
  "description": "Instagram Story Song Suggester using Gemini AI and Spotify",
  "main": "index.js",
  "scripts": {
    "build": "cd netlify/functions && npm install",
    "dev": "python app.py"
  },
  "dependencies": {
    "@netlify/functions": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
